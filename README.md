# watchdog

```bash
npm install
npm run start

# other shell
curl -X "POST" --data '{"jobName":"job-name-#1", "arguments":["arg1"]}' -H "Content-Type: application/json" localhost:3000/jobs
curl localhost:3000/jobs
curl localhost:3000/stats
```